# Ignore snapshots/ (historical data backups, kept locally only)
sales-center/data/snapshots/
sales-center-mobile/data/snapshots/

# Ignore archive directory (legacy files moved out of sales-center/)
archive/

# 性能优化备份（仅本地保留）
.freeze_*

# macOS 系统文件
.DS_Store

# 各类 .bak 备份（仅本地保留，不入 git）
*.bak
*.bak_*
